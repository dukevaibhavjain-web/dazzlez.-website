{
  "buildCommand": "pnpm run migrate:ci || true && pnpm run build",
  "installCommand": "pnpm install",
  "functions": {
    "src/app/api/**": { "maxDuration": 60 },
    "src/app/(payload)/**": { "maxDuration": 60 }
  }
}
